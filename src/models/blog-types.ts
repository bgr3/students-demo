export type BlogType = {
    id:	number,
    name: string,
    description: string,
    websiteUrl: string,
}

export type BlogPostType = {
    name: string,
    description: string,
    websiteUrl: string,
}

export type BlogPutType = {
    name: string,
    description: string,
    websiteUrl: string,
}